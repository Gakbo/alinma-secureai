python-dotenv>=1.0.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic[email]==2.9.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt 5.x removed __about__ which passlib 1.7.4 reads -> crash on hashing.
bcrypt==4.0.1
python-multipart==0.0.9
psycopg2-binary==2.9.9

# AI chatbot (rule-based fallback works without a key; see OPENAI_API_KEY)
openai>=1.0.0

# ML dependencies (TF-IDF SMS tier + XGBoost transaction model)
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.0.0

# Tests (backend/tests): pytest + httpx power FastAPI's TestClient
pytest>=8.0.0
httpx>=0.27.0

# SMS model (Model A transformer — optional, ~1.1 GB). For serving, install
# the CPU build FIRST:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Plain `pip install torch` pulls the ~2.5GB CUDA wheel you don't need.
torch
transformers
